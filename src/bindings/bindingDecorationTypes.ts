/** Данные для бейджа и tooltip привязки ИБ на узле Configuration (plan §2C). */

export type ConfigurationBindingDecoration = {
  readonly boundCount: number;
  readonly namesPreview: string;
  readonly massDeployment: boolean;
};

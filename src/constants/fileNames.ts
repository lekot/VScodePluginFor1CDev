/**
 * Canonical file names used across the 1C metadata tree extension.
 */

/** Root configuration descriptor present in both Designer and EDT layouts. */
export const CONFIGURATION_XML = 'Configuration.xml';

/** Designer XML dump descriptor. */
export const CONFIG_DUMP_INFO_XML = 'ConfigDumpInfo.xml';

/** Binary configuration package extensions. */
export const CONFIGURATION_PACKAGE_EXTENSIONS = ['.cf', '.cfe'] as const;

/** Form structure file inside a form's Ext directory. */
export const FORM_XML = 'Form.xml';

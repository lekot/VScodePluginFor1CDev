/**
 * Имена из фикстуры `FormatSamples/empty_conf` для смоков матрицы / ibcmd.
 * Не использовать в `src/` — только в тестах и сценариях запуска.
 */
export const SMOKE_EMPTY_CONF_RECORDER_DOCUMENT = 'ДокументТестРаботает';
